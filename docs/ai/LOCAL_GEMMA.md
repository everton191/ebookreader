# Gemma local

Gemma 4 E2B e' baixado por demanda para `files/local-models`. O instalador usa
`.part`, Range HTTP, tamanho esperado, SHA-256 e rename atomico. O provider e'
fail-closed: se o arquivo nao estiver integro, nenhuma chamada vai para
provedores de rede.
